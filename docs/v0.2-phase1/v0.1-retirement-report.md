# v0.1 retirement verification

The exact retired sources are `retired-v0.1/policy.json` and
`retired-v0.1/memory-policy.json`. The replacement authorities are
`loopeng/audit/policy.py` and `loopeng/okf/schema.py`.

## Policy mapping

| Retired entry | Current entry | Result |
| --- | --- | --- |
| deny 1 through 8 | current deny 1 through 8 | all equivalent |
| high-risk 1 through 10 | current high-risk 1 through 10 | all equivalent |

Matching is regex-based, preserving the formerly weakened cases.

## Memory mapping

| Retired item | Current authority | Result |
| --- | --- | --- |
| allowed types except Loop Brief Pattern | `schema.ALLOWED_TYPES` | present |
| Loop Brief Pattern | none | intentionally not adopted because the surface is retired |
| active/deprecated statuses | `schema.ALLOWED_STATUSES` | present and validated |
| public/internal sensitivity | `schema.ALLOWED_SENSITIVITY` | present and validated |
| required frontmatter | `schema.REQUIRED_FRONTMATTER` | all present |

Retained templates: `OKF_CONCEPT.md`, `SOP_SKILL_TEMPLATE.md`, and
`INSTALL_MERGE_REPORT.md`. The old Loop Brief templates are removed.
